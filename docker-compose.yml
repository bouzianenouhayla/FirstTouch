services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file: .env

  web:
    build: ./ts-client
    ports:
      - "3000:3000"
    depends_on:
      - api
    environment:
      API_URL: http://api:8000
